gnome-terminal -- bash -c "cd ../server && bash whileExecHot clientId clientSecret password doesntMatter user TimeOfExecution(inSeconds)"
gnome-terminal -- bash -c "cd ../server && bash whileExecTop clientId clientSecret password doesntMatter user TimeOfExecution(inSeconds)"
gnome-terminal -- bash -c "cd ../server && bash whileExecServer clientId clientSecret password doesntMatter user TimeOfExecution(inSeconds)"
gnome-terminal -- bash -c "cd ../server && bash whileExecServer clientId clientSecret password doesntMatter user TimeOfExecution(inSeconds)"
gnome-terminal -- bash -c "cd ../server && bash whileExecServer clientId clientSecret password doesntMatter user TimeOfExecution(inSeconds)"
gnome-terminal -- bash -c "bash linuxExecReaders"
